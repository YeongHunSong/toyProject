
image:
  dir: C:\Users\car06\Desktop\image\

spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=MySQL;
    username: sa
    password:

  profiles:
    active: test

#MyBatis
mybatis:
  type-aliases-package: syh.toyProject.domain

  configuration:
    map-underscore-to-camel-case: true

  mapper-locations: classpath:mapper/**/*.xml

logging:
  level:
    org:
      springframework:
        jdbc: debug

    syh:
      toyProject:
        repository:
          mybatis: trace